# 🎥 Админ панель для загрузки видео - Инструкция

Полная система управления обучающими видео через веб-интерфейс.

---

## ✅ Что реализовано:

1. **SQL миграция** - настройка базы данных и Storage
2. **Админ панель** - страница `/admin-tutorials` для загрузки видео
3. **Backend API** - endpoints для upload/delete видео
4. **Просмотр в боте** - пользователи Telegram смотрят видео в `/tutorials`
5. **Автоматический прогресс** - отслеживание просмотренных видео

---

## 📋 Шаг 1: Настройка базы данных

### Выполни SQL код в Supabase Dashboard

**Dashboard → SQL Editor → New Query**

Скопируй и вставь этот код, затем нажми **RUN**:

```sql
-- ===================================
-- ПОЛНАЯ СИСТЕМА ВИДЕО-ТУТОРИАЛОВ
-- ===================================

-- 1. Создать Storage Bucket для видео (если еще не создан)
INSERT INTO storage.buckets (id, name, public)
VALUES ('tutorials', 'tutorials', false)
ON CONFLICT (id) DO NOTHING;

-- 2. Настроить политики доступа к Storage
-- Политика: Админы могут загружать видео
DROP POLICY IF EXISTS "Allow service role to upload tutorials" ON storage.objects;
CREATE POLICY "Allow service role to upload tutorials"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'tutorials');

-- Политика: Админы могут удалять видео
DROP POLICY IF EXISTS "Allow admins to delete tutorials" ON storage.objects;
CREATE POLICY "Allow admins to delete tutorials"
ON storage.objects FOR DELETE
TO authenticated
USING (bucket_id = 'tutorials');

-- Политика: Все могут читать видео
DROP POLICY IF EXISTS "Allow authenticated users to read tutorials" ON storage.objects;
CREATE POLICY "Allow authenticated users to read tutorials"
ON storage.objects FOR SELECT
TO authenticated
USING (bucket_id = 'tutorials');

-- 3. Создать таблицу tutorials (метаданные видео)
CREATE TABLE IF NOT EXISTS tutorials (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  description TEXT,
  duration INTEGER NOT NULL,
  thumbnail_url TEXT,
  storage_path TEXT NOT NULL,
  order_index INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_tutorials_order ON tutorials(order_index);

-- 4. Создать таблицу user_video_progress (прогресс просмотра)
CREATE TABLE IF NOT EXISTS user_video_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  tutorial_id UUID NOT NULL REFERENCES tutorials(id) ON DELETE CASCADE,
  watched BOOLEAN DEFAULT FALSE,
  last_position INTEGER DEFAULT 0,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, tutorial_id)
);

CREATE INDEX IF NOT EXISTS idx_user_video_progress_user ON user_video_progress(user_id);
CREATE INDEX IF NOT EXISTS idx_user_video_progress_tutorial ON user_video_progress(tutorial_id);

-- 5. Функция для автоматического обновления updated_at
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS update_tutorials_updated_at ON tutorials;
CREATE TRIGGER update_tutorials_updated_at
BEFORE UPDATE ON tutorials
FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

DROP TRIGGER IF EXISTS update_user_video_progress_updated_at ON user_video_progress;
CREATE TRIGGER update_user_video_progress_updated_at
BEFORE UPDATE ON user_video_progress
FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

-- 6. Включить Row Level Security для tutorials
ALTER TABLE tutorials ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can read tutorials"
ON tutorials FOR SELECT
TO authenticated
USING (true);

CREATE POLICY "Admins can manage tutorials"
ON tutorials FOR ALL
TO authenticated
USING (true)
WITH CHECK (true);

-- ГОТОВО! ✅
```

---

## 🚀 Шаг 2: Деплой Backend

**Важно:** Backend должен быть задеплоен для работы загрузки!

### Если есть CLI:

```bash
cd supabase/functions
supabase functions deploy make-server-4c493c62
```

### Если нет CLI:

Используй **GitHub Integration**:
- Dashboard → Edge Functions
- "Deploy from GitHub"
- Выбери свой репозиторий

---

## 👨‍💼 Шаг 3: Войти как админ

### Создай админа (если еще не создал):

**Dashboard → Authentication → Users → Add User**
- Email: `admin@test.local`
- Password: (твой пароль)
- ✅ Auto Confirm User

### Войди в приложение:

1. Открой приложение в браузере
2. Используй email/password админа
3. После входа увидишь в Sidebar новый раздел **"Администрирование"**

---

## 📤 Шаг 4: Загрузка видео

### 4.1 Открой админ панель

В приложении:
- **Sidebar → Администрирование → Управление видео**
- Или перейди на `/admin-tutorials`

### 4.2 Заполни форму

**Поля формы:**
- **Название видео** - Заголовок (обязательно)
- **Описание** - Краткое описание урока
- **Видео файл** - Выбери файл (MP4, WebM, MOV)

После выбора файла **длительность определится автоматически**!

### 4.3 Нажми "Загрузить видео"

Процесс:
1. ⏳ Файл загружается в Supabase Storage
2. 📊 Создается запись в таблице `tutorials`
3. ✅ Видео сразу доступно всем пользователям!

---

## 👥 Шаг 5: Просмотр в Telegram боте

### Для пользователей Telegram:

1. **Открывают бота** в Telegram
2. **Заходят в раздел "Обучение"** (или `/tutorials`)
3. **Видят список всех загруженных видео**
4. **Смотрят видео** с автоматическим сохранением прогресса

### Что видят пользователи:

- ✅ Список всех видео с описанием
- ✅ Прогресс просмотра (✓ просмотрено или % прогресс)
- ✅ Видеоплеер с управлением
- ✅ Навигация Вперед/Назад
- ✅ Авто-переход к следующему видео

---

## 🗑️ Удаление видео

### В админ панели:

1. Найди видео в списке
2. Нажми **иконку корзины** (🗑️)
3. Подтверди удаление
4. Видео удаляется из:
   - ✅ Supabase Storage (файл)
   - ✅ Таблицы `tutorials` (запись)
   - ✅ Приложения (сразу исчезает у пользователей)

---

## 📊 Как это работает

### Архитектура:

```
Админ (Web)
    ↓ загружает видео
Supabase Storage (S3)
    ↓ хранит файлы
Таблица tutorials
    ↓ метаданные
Backend API
    ↓ генерирует signed URLs
Telegram Bot
    ↓ показывает видео
Пользователи
    ↓ смотрят
user_video_progress
    ↓ сохраняет прогресс
```

### Процесс загрузки:

1. **Админ** выбирает файл в браузере
2. **Frontend** отправляет FormData на backend
3. **Backend** загружает файл в Storage bucket `tutorials`
4. **Backend** создает запись в таблице `tutorials` с метаданными
5. **Frontend** обновляет список видео

### Процесс просмотра:

1. **Пользователь** открывает `/tutorials`
2. **Frontend** запрашивает список с прогрессом
3. **Backend** возвращает:
   - Метаданные видео
   - Прогресс пользователя (watched/position)
4. **Frontend** запрашивает signed URL для видео
5. **Backend** генерирует временную ссылку (1 час)
6. **Видеоплеер** показывает видео
7. **Каждые 5 секунд** сохраняется позиция просмотра
8. **При завершении** видео отмечается как просмотренное

---

## 🔒 Безопасность

### Что защищено:

✅ **Storage bucket приватный** - файлы не доступны напрямую  
✅ **Signed URLs** - ссылки работают только 1 час  
✅ **RLS политики** - только авторизованные могут создавать/удалять  
✅ **Роли** - админ панель только для `role === 'admin'`  
✅ **Backend проверка** - Service role key для всех операций

### Кто что может:

| Действие | Админ (Web) | Пользователь (Telegram) |
|----------|-------------|-------------------------|
| Загрузка видео | ✅ Да | ❌ Нет |
| Удаление видео | ✅ Да | ❌ Нет |
| Просмотр списка | ✅ Да | ✅ Да |
| Просмотр видео | ✅ Да | ✅ Да |
| Сохранение прогресса | ✅ Да | ✅ Да |

---

## 🐛 Troubleshooting

### Ошибка: "Failed to upload video"

**Решение:**
- ✅ Проверь что Edge Functions задеплоены
- ✅ Проверь что SQL миграция выполнена
- ✅ Проверь размер файла (рекомендуется до 100MB)
- ✅ Проверь логи: Dashboard → Edge Functions → Logs

---

### Ошибка: "Access denied"

**Решение:**
- ✅ Залогинься как админ (email/password)
- ✅ Проверь что `user.role === 'admin'` (в профиле должен быть badge "Администратор")
- ✅ Если нет badge - проверь логику создания админа

---

### Видео не появляется у пользователей

**Решение:**
- ✅ Проверь что запись создалась: Dashboard → Table Editor → tutorials
- ✅ Проверь что файл загрузился: Dashboard → Storage → tutorials
- ✅ Попроси пользователя обновить страницу
- ✅ Проверь browser console (F12) на ошибки

---

### Signed URL expired

**Решение:**
- Это нормально! URLs действительны 1 час
- Пользователь должен обновить страницу
- Новый URL сгенерируется автоматически

---

## 📈 Статистика (будущее)

В будущем можно добавить:

- 📊 Dashboard с аналитикой
- 👥 Сколько пользователей посмотрело каждое видео
- ⏱️ Средняя длительность просмотра
- 📉 Drop-off points (где пользователи уходят)
- 📋 Экспорт отчетов

---

## 🎉 Готово!

Теперь у тебя:

✅ **Админ панель** для загрузки видео  
✅ **Безопасное хранение** в Supabase Storage  
✅ **Автоматический показ** в Telegram боте  
✅ **Отслеживание прогресса** для каждого пользователя  
✅ **Простое управление** - загрузил и всё работает!

---

**Загружай видео и обучай пользователей! 🚀**
